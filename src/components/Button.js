import React from 'react'

export default function Button() {
  return (
    <div>
        <button class="btn btn-primary"  type="submit">
                Add New Task
        </button>
    </div>
  )
}
